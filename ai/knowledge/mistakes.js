export const MISTAKES = `
═══════════════════════════════════════════════
COMMON MISTAKES — NEVER DO THESE
═══════════════════════════════════════════════

1. Making the entire post bold
   - Bold is ONLY for key terms, not whole paragraphs

2. Using 3+ emojis in a row
   - Max 1-2 emojis together
   - Never 🔥🔥🔥 or 😍😍😍

3. Removing GitHub repository links
   - NEVER remove technical links
   - If a post mentions a repo, the link MUST appear in output

4. Not removing advertisements
   - "Join our channel" must be removed
   - "@DevTwitter | Author" must be removed
   - t.me/joinchat links must be removed

5. Adding quotes without reason
   - Quote blocks are for URLs, commands, footer
   - NOT for decorating random sentences

6. Rewriting when only formatting was needed
   - If the text is already good, just format it
   - Don't change words unnecessarily

7. Adding footer twice
   - Footer appears ONCE at the end
   - The formatter adds it, AI should NOT

8. Making all posts look identical
   - Don't use a template (heading → emoji → paragraph → link → footer)
   - Every post should have its own visual identity

9. Adding unnecessary headings
   - Only add headings when they improve navigation
   - Don't add "Introduction" or "Conclusion" headings

10. Making all links bold
    - Links go in blockquotes, not bold
    - Only bold the text that introduces the link

11. Translating Persian to English
    - NEVER translate unless explicitly forced
    - Persian input → Persian output

12. Using hype words
    - Never use "amazing", "revolutionary", "شگفت‌انگیز"
    - Use natural, professional language

13. Changing the author's emotional tone
    - If the post is excited, keep it excited
    - If the post is serious, keep it serious
    - Don't flatten emotion into dry text

14. Splitting code blocks
    - Never split a code block across paragraphs
    - Keep code blocks intact

15. Adding "Here is the rewritten post:" prefix
    - Never add meta-commentary
    - Return ONLY the post text
═══════════════════════════════════════════════
`.trim();
