/**
 * ai/examples/tutorials.js
 */

export const EXAMPLES_TUTORIALS = `
--- Tutorial Example 1 ---
INPUT:
To install first run npm install then run npm start and its super amazing!! Also don't forget to set NODE_ENV

OUTPUT:
To install, first run npm install, then run npm start. Also set the NODE_ENV variable.

--- Tutorial Example 2 ---
INPUT:
برای نصب اول pip install رو اجرا کن بعد python main.py رو اجرا کن خیلی راحته!

OUTPUT:
برای نصب، ابتدا pip install را اجرا کنید، سپس python main.py را اجرا کنید.

--- Tutorial Example 3 ---
INPUT:
Step 1 clone the repo step 2 install deps step 3 run the server its that easy lol

OUTPUT:
1. Clone the repository
2. Install dependencies
3. Run the server

--- Tutorial Example 4 ---
INPUT:
اول docker build کن بعد docker run کن و تمام!

OUTPUT:
1. ابتدا docker build را اجرا کنید
2. سپس docker run را اجرا کنید

--- Tutorial Example 5 ---
INPUT:
to setup first install node then install npm packages then create config file then run the app

OUTPUT:
1. Install Node.js
2. Install npm packages
3. Create a config file
4. Run the application

--- Tutorial Example 6 ---
INPUT:
برای استفاده اول ثبت‌نام کن بعد API key بگیر بعد تو کدت استفاده کن

OUTPUT:
1. ابتدا ثبت‌نام کنید
2. سپس API key دریافت کنید
3. در کد خود از آن استفاده کنید

--- Tutorial Example 7 ---
INPUT:
run this command its awesome: npm install --save my-package

OUTPUT:
Run this command: npm install --save my-package

--- Tutorial Example 8 ---
INPUT:
برای اجرا اول git clone کن بعد cd کن به پوشه بعد npm install کن

OUTPUT:
برای اجرا:
1. ابتدا git clone را اجرا کنید
2. سپس وارد پوشه شوید (cd)
3. در نهایت npm install را اجرا کنید
`;
